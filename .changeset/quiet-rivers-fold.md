---
"@minpeter/ai-router": patch
---

Reorganize the source tree: group providers into per-provider folders under `src/provider/` and shared core modules under `src/core/`. Pure structural move — no public API, behavior, or build-output change.
